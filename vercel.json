{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm install -g pnpm@10.32.1 && pnpm --dir crates/agent-gateway/web install --frozen-lockfile",
  "buildCommand": "npm install -g pnpm@10.32.1 && pnpm --dir crates/agent-gateway/web build",
  "outputDirectory": "crates/agent-gateway/web/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
